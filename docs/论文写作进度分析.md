# 论文写作进度分析

**更新日期**：2026-02-26
**论文主题**：面向多用户多关键词动态 SSE 的可验证性机制研究

---

## 已完成部分

### 第一章（引言）— 完整

- 研究背景与动机
- 相关工作（三个子方向：动态 SSE、多用户多关键词 SSE、可验证 SSE）
- 研究目标与创新点
- 论文结构说明
- 方案对比表（Table 1.1）

### 第二章（预备知识 + MHT 机制）— 完整

- 系统模型、威胁模型、安全目标定义
- Nomos 基线方案描述
- BF 结构的局限性分析
- 基于 MHT 的可验证资格检查机制（QTree）
- 命题 1–4 及完整的游戏跳跃安全证明

### 第三章（嵌入式承诺机制）— 完整

- 地址绑定问题的形式化
- 嵌入式承诺构造（AE 升级为 INT-CTXT）
- 命题 5（地址绑定可靠性）与命题 6（联合可验证性）
- 与现有 VSSE 方案的对比表（Table 3.1）

---

## 缺失部分（关键）

### 第四章：实验与性能分析 — 完全空白

当前最大缺口，需补充以下内容：

**4.1 实验环境**
- 硬件配置（CPU/内存/存储）
- 操作系统、编程语言与库版本

**4.2 数据集描述**
- 文档集规模 $N$、关键词集大小 $|\mathcal{W}|$、每关键词平均文档数 $|\text{DB}(w)|$

**4.3 QTree 构造与更新开销**
- $\text{BuildIndex}$ 时间 vs. 基线 BF 方案
- $\text{Update}$ 操作（插入/删除）时间，重点是 MHT 路径更新的 $O(\log M)$ 开销

**4.4 搜索与证明生成时间**
- $\text{GenToken}$ 时间
- 服务端 $\text{Search}$ + 证明路径生成时间
- 客户端 $\text{Verify}$ 时间（路径验证 + 承诺开启）

**4.5 存储开销分析**
- 服务端：$O(N\lambda)$ 承诺存储 + $O(M \log M)$ MHT 节点
- 客户端：根哈希 $\text{rt}$ + 承诺密钥

**4.6 通信开销分析**
- 每次搜索的证明大小：$k \cdot \log M$ 路径节点 + $\ell$ 个 xtag 开启值

**4.7 与现有方案对比**
- 对应 Table 3.1 中已列出的 Sun15、Liu20、Zhang19 等方案，给出实测数据对比

### 第五章：总结与展望 — 完全空白

需补充：

1. **研究总结**：对三个核心贡献的简洁复述（QTree 机制、嵌入式承诺、联合可验证性）
2. **局限性说明**：MHT 更新的摊销代价、多用户密钥管理复杂度
3. **未来工作**：标准模型下的可验证 SSE、支持范围查询的可验证结构、后量子安全扩展

---

## 次要问题

- `cryptographic_primitives.tex` 与 `bf.tex` 中系统模型定义存在内容重叠，建议统一到预备知识章节，`bf.tex` 中直接引用
- `bf.tex` 中符号表（Table 2.1）与 `commitment.tex` 中符号表（Table 3.1）格式不一致，建议统一

---

## 优先级建议

第四章实验是最紧迫的缺口——没有实验数据，论文无法通过答辩。建议先确定实验实现方案（是否有可运行代码），再推进第五章写作。
